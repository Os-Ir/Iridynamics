package com.atodium.iridynamics.common.level;

import net.minecraft.server.level.ServerLevel;
import net.minecraftforge.common.world.ForgeChunkManager;

public class ModChunkManager implements ForgeChunkManager.LoadingValidationCallback {
    @Override
    public void validateTickets(ServerLevel level, ForgeChunkManager.TicketHelper ticketHelper) {

    }
}